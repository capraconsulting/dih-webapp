## circle.yml
machine:
    node:
        version: 4.2.1
    environment:
        NODE_ENV: test
dependencies:
    pre:
        - npm install -g npm
        - npm install
        - npm run selenium install
        - npm run dev
            background: true
test:
    override:
        - npm test
