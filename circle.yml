## circle.yml
machine:
    node:
        version: 4.2.1
    environment:
        NODE_ENV: test
dependencies:
    pre:
        - npm install -g npm
        - npm install
        - npm run selenium install
    post:
        - java -jar selenium-server-standalone-2.44.0.jar:
            background: true
test:
    override:
        - npm test
