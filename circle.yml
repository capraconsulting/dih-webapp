## circle.yml
machine:
    node:
        version: latest
    environment:
        NODE_ENV: test
dependencies:
    pre:
        - npm install -g npm
        - npm install
        - npm run selenium install
test:
    override:
        - npm test
